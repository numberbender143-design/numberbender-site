User-agent: *
Allow: /

# NOTE (2026-06 crawlability audit): the old .html URLs are intentionally NOT
# disallowed here. They 301 to the clean URLs — Google must be able to crawl
# them to follow the redirects and consolidate ranking signals. Blocking a
# redirecting URL freezes it in the index instead of letting it consolidate.
#
# Thank-you / confirmation pages are likewise NOT blocked here: they carry
# <meta name="robots" content="noindex, nofollow">, which Google can only
# obey if it is allowed to crawl the page.

# Parameterized URLs that produce duplicate content
Disallow: /courses?s=
Disallow: /contact?

Sitemap: https://numberbender.com/sitemap.xml
Sitemap: https://numberbender.com/video-sitemap.xml
